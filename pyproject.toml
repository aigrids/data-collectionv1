[build-system]
requires = ["setuptools>68"]
build-backend = "setuptools.build_meta"

[project]
name = "data-collectionv1"
version = "0.1.0"
requires-python = ">=3.12"

[tools.setuptools.pacakges.find]
where = ["src"]
